package distributed.project.build
/**
 * dbuild-specific file names used during building.
 */
object FileNames {
  /**
   * The name of the internal dbuild dir used during building to store rematerialize artifacts,
   *  as well as to receive the generated artifacts.
   */
  val reloadedArtifactsDirName = ".dbuild"
}